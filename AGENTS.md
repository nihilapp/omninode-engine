# 프로젝트 운영 지침

## 기본 원칙

- 사용자를 마스터로 호칭하고, 진중하고 차분한 전문적 어조의 한글로 응답한다.
- 작업을 시작하기 전에 이 파일과 작업 범위에 관련된 `docs/`, `references/`, 기존 구현을 읽는다.
- 적용 가능한 Skill을 먼저 확인하고 사용한다. 특히 비경량 작업에는 Superpowers 절차를 기본으로 적용한다.
- 명시된 요구사항과 이 문서의 규칙이 충돌하면 마스터의 최신 지시를 우선한다.

## 기획 구체화와 보강 절차

마스터가 서비스의 기초 기획을 제공하고 에이전트를 통해 구체화하려는 경우, 그 기획은 최종 명세가 아닌 1차 기획을 만들기 위한 출발점으로 취급한다.

- 마스터가 참고 폴더나 문서를 지정하면, 질문 전에 먼저 확인한다.
- 한 번에 하나의 결정 단위만 묻는다. 서로 강하게 연결된 내용은 하나의 묶음으로 다루되, 무관한 질문을 함께 섞지 않는다.
- 선택지가 도움이 되는 경우에는 이해하기 쉬운 선택지를 제공한다.
- 이미 확정한 결정에서 바로 따라오는 내용은 다시 선택지로 묻지 않는다. 파생 규칙으로 정리해 기록한다.
- 마스터가 선택하거나 기존 결정을 정정하면 즉시 `docs/project-design/`의 결정 기록에 반영한다.
- 기획 구체화의 진행 규칙, 질문 원칙, 보류 항목은 `docs/work-design/`에 기록한다.
- 기록 뒤에는 `git diff --check`로 문서 형식 오류를 확인하고, 반영한 내용을 짧게 알린 뒤 다음 한 가지 질문만 제시한다.
- 확정되지 않은 내용은 확정으로 기록하지 않고, `미결 결정`으로 분리한다.
- 이 절차의 결과는 1차 기획이다. 구현, API, 데이터베이스 변경은 별도 구현 승인이 있기 전까지 수행하지 않는다.

기획 보강은 `1차 기획 확정 결정 → 사용자 스토리 → 유스케이스 → 데이터 플로우 → 데이터 스키마` 순으로 진행한다. 사용자 스토리는 `누가, 무엇을, 왜`를, 유스케이스는 그 목적을 달성하는 사용자 행동과 시스템 반응의 흐름을, 데이터 플로우는 그 흐름의 데이터 읽기·검증·생성·변경·보존을 다룬다.

- 유스케이스는 하나의 명확한 사용자 목적과 행위자를 다루며, 하나 이상의 확정 `US-번호`를 반드시 기록한다.
- 유스케이스에는 필요한 범위에서 이름, 행위자, 목적, 사전 조건, 기본 흐름, 대안 흐름, 예외 흐름, 결과, 관련 데이터를 기록한다.
- 유스케이스에는 화면 배치·컴포넌트·버튼 위치, API·HTTP 메서드·DB 쿼리·ORM·프레임워크 등 UI·기술 구현을 넣지 않는다. 데이터 구조도 미리 확정하지 않는다.
- 유스케이스 중 정책 결정이 필요하면 임의로 정하지 않는다. 지금 결정해야 하면 마스터에게 하나의 결정 단위로 질문하고, 그렇지 않으면 `미결 사항`으로 분리한다.
- 데이터 플로우는 대응하는 `UC-번호`와 `US-번호`를 기록하고, 유스케이스에서 확정된 행동과 시스템 반응만을 근거로 작성한다.
- 기본·대안·예외 흐름에 따라 데이터 처리가 실질적으로 달라지면 데이터 플로우에도 그 차이를 반영한다. 새 행동이나 제품 정책을 임의로 추가하지 않는다.
- 스키마 초안을 작성한 뒤, 3차 보강을 확정하기 전에 테이블 간 참조·범위·삭제 처리·제약을 점검한다. 이 점검을 별도 다음 단계로 두지 않는다.
- 상위 근거 없는 유스케이스·데이터 플로우·데이터 구조 또는 하위 단계에 빠진 확정 내용을 발견하면 누락·임의 추가 여부를 점검하고, 새 정책이 필요하면 필요한 결정 단계로 돌아가 마스터와 확정한다.

3차 보강과 유스케이스 후행 작성 정합성 점검을 마친 뒤에는, 프론트 라우트와 API 엔드포인트 설계 전에 4차 기획을 진행한다. 순서는 `도메인 책임 정의 → 권한 매트릭스 → 검증 규칙 → 실패 조건 → 상태 정의 및 전이`다.

- 도메인 책임에서는 각 도메인이 소유하는 개념·상태·행동과 도메인 경계를 정리하며, 화면·API·테이블 구현을 미리 정하지 않는다.
- 권한 매트릭스에서는 역할·범위별 조회·생성·수정·삭제·특수 행동의 허용 여부를 확정한다.
- 검증 규칙에서는 입력값, 범위, 상태 전이, 중복, 참조 정합성의 성공 조건을 확정한다.
- 실패 조건에서는 권한 부족, 입력 오류, 상태 충돌, 삭제·비공개·비활성 대상 접근의 시스템 결과를 확정한다.
- 상태 정의 및 전이에서는 각 도메인의 상태값, 최초 상태, 허용 전이, 전이 주체, 전이 결과와 전이 금지 조건을 확정한다.
- 각 하위 단계를 마칠 때마다 확정 제품 규칙은 `docs/project-design/`에, 진행 수칙은 `docs/work-design/`과 프로젝트·사용자 에이전트 지침에 즉시 기록한다. 직접적인 파생 결과는 재질문하지 않고, 새 비즈니스·기획 판단이 필요한 경우에만 마스터에게 한 결정 단위로 묻는다.
- 도메인 책임 문서에는 도메인명, 소유 개념·상태, 책임 행동, 다른 도메인과의 경계, 횡단 정책의 소유자를 기록한다. 한 상태를 둘 이상의 도메인이 직접 바꾸는 것으로 적지 않고, 도메인 간 영향은 상태 전이 또는 참조 규칙으로 명확히 한다.
- 권한 매트릭스에는 역할별 기본 범위, 관리 대상·행동별 허용 여부, 24개 허가 묶음과 실제 행동의 연결, 특수 권한 규칙을 기록한다. 역할 이름과 세부 허가의 책임을 섞지 않으며, 문서 허가에 포함되는 부수 행동처럼 예외가 있으면 명시한다.
- 검증 규칙에는 공통 권한·범위·상태 조건과 도메인별 생성·수정·삭제·복구·공개 행동의 성공 조건을 기록한다. 입력 형식, 중복 기준, 참조 범위, 상태 전이, 사용 중지·삭제 상태의 제약을 행동별로 분리하고, 실패 결과 자체는 다음 실패 조건 단계에서 정의한다.
- 실패 조건에는 검증 실패 시 바뀌지 않아야 할 상태, 권한·범위·입력·중복·참조·상태 충돌별 실패 조건, 접근 불가·선택 불가·기존 표기·404 같은 시스템 결과를 기록한다. 화면 문구, HTTP 상태 코드, API 오류 형식은 이 단계에서 정하지 않는다.
- 상태 정의 및 전이 문서에는 상태를 가진 각 도메인, 최초 상태, 허용 전이, 전이 주체와 결과, 전이 금지 조건을 기록한다. 공통 `use_yn`·`del_yn` 같은 횡단 상태와 도메인 고유 상태를 구분하며, 하나의 행동이 여러 상태를 바꿀 때에는 상태별 책임과 순서를 명확히 한다.

## 프론트 라우트·API 설계 절차

- 프론트 라우트와 API 설계는 4차 기획을 마친 뒤 확정된 도메인 책임·권한·검증·실패·상태 규칙을 근거로 진행한다.
- 페이지 설계에는 페이지별 사용자, 진입 경로, 핵심 데이터, 주요 행동, 권한 필터, 주요 이동 경로를 기록하고 모든 확정 유스케이스를 하나 이상의 페이지와 API에 연결한다.
- `/admin`, `/admin/worlds/[worldId]`, `/admin/projects/[projectId]`는 각각 전역·월드·프로젝트의 행동 중심 대시보드다. 상태와 할 일만 보여 주지 않고 간단한 생성·수정·삭제·복구를 직접 제공하며, 복잡한 목록과 편집은 전문 관리 페이지로 연결한다.
- 단순한 계정·월드·프로젝트·초대·태그 입력은 대시보드나 목록의 대화상자·드로어에서 처리하고, 템플릿·문서·관계 편집과 문서 이동은 독립 페이지로 둔다.
- API는 `/api/v1/auth`, `/api/v1/management`, `/api/v1/public`으로 구분한다. 이 세 구간은 네임스페이스이며, 그 아래 리소스와 상태 전이 사건의 경로명은 모두 복수형으로 작성한다.
- 관리 API는 내부 UUID를 사용하고 공개 API는 월드·프로젝트 슬러그와 문서상 이름을 사용한다. 공개 경로와 응답에는 내부 UUID를 연결 식별자로 노출하지 않는다.
- API 성공과 실패 응답은 모두 `data`, `error`, `code`, `message`, `details`를 가지며 `details`를 생략하지 않는다. 부가 정보가 없으면 `null`을 사용한다.
- 문서·템플릿 저장은 `baseRevisionId`로 충돌을 검사하고, 복원·활성화·비활성화·이동·공개 같은 중요 상태 전이는 복수형 사건 리소스로 분리한다.
- 라우트·API 설계의 상세 기준은 `docs/project-design/2026-09-03-omninode-route-api-design.md`를 따른다. 설계 확정과 문서화는 라우트 파일, API 핸들러, 타입, 테스트, ORM, 마이그레이션 또는 DB 변경의 구현 승인을 뜻하지 않는다.

## 작업 분류와 진행 절차

- 실제 수정·생성·삭제 대상 파일이 하나면 경량 작업이다.
- 대상 파일이 둘 이상이면 비경량 작업이다.
- 경량 작업은 현재 세션에서 직접 처리한다. 별도의 실행 방식 선택을 요청하지 않는다.
- 경량 작업도 변경 범위에 맞는 최소 검증을 실행한 후 결과를 보고한다.
- 비경량 작업은 다음 순서를 따른다.
  1. 만들 앱이나 기능의 목적, 사용자, 해결할 문제, 완료 기준을 마스터에게 확인한다.
  2. `superpowers:brainstorming`으로 콘셉트와 범위를 정리하고 설계 승인을 받는다.
  3. 설계 문서를 `docs/superpowers/specs/`에 작성한다.
  4. `superpowers:writing-plans`으로 구현 계획을 `docs/superpowers/plans/`에 작성한다.
  5. 승인된 계획에 따라 구현한다.
  6. 테스트·린트·타입 검사·빌드 중 영향 범위에 적합한 검증을 실행하고, 결과를 근거와 함께 보고한다.
- 이 저장소는 세계관 설정을 구조화하고 연결해 관리·공개하는 지식 아카이브 `옴니노드`의 제품 저장소다. 새 기능 개발은 확정된 제품 설계와 작업 설계를 근거로 진행한다.

## 문서 관리

- 서비스 전체에 지속적으로 적용되는 제품 원칙, 도메인 구조, 권한, 데이터 보존 규칙, 확정 요구사항은 `docs/project-design/` 아래 Markdown으로 작성한다.
- 특정 기능이나 변경 작업의 범위, 화면·데이터·API 영향, 구현 순서, 검증과 완료 기준은 `docs/work-design/` 아래 Markdown으로 작성한다.
- `docs/superpowers/` 문서는 Superpowers 절차에 따라 별도로 관리한다. 제품 설계와 작업 설계 문서는 이 폴더의 문서를 대체하지 않는다.
- `references/`는 개발 중 참고할 자료를 위한 폴더다. 마스터가 제공한 문서, 요구사항, 조사 자료, 외부 레퍼런스를 Markdown으로 보관한다.
- 에이전트가 작성한 분석이나 정리도 마스터가 향후 참고할 가치가 있다고 판단할 때에만 `references/`에 보관한다.
- 지속적으로 적용되는 작업 규칙은 `AGENTS.md`에 기록하고, 특정 기능의 상세 근거는 설계 문서 또는 참고 문서에 둔다.
- 공개 개발 현황의 단일 기준은 `docs/work-design/omninode-progress.md`다. 의미 있는 설계·구현·검증 작업을 마칠 때마다 현재 단계, 완료 범위, 다음 작업, 검증 근거와 업데이트 이력을 갱신한다.
- 개발 현황에서는 설계 완료와 구현 완료를 구분한다. 구현 산출물과 해당 범위의 검증 결과가 모두 확인된 경우에만 구현 항목을 완료로 기록한다.
- 정식 개발 순서는 `docs/work-design/2026-09-04-omninode-development-roadmap.md`를 따른다. 데이터·인증·권한 기반을 먼저 구축하고, 이후 단계에서는 도메인별 API와 페이지를 함께 완성한다.
- 개발 로드맵의 각 단계는 별도의 설계 승인, 구현 계획, 구현과 검증 단위로 진행한다. 장기 순서를 바꾸거나 단계를 합칠 때에는 마스터의 결정을 먼저 받는다.

## 프로젝트 구조

- 루트 구조는 `app/`, `server/`, `public/`을 기본으로 한다.
- `app/` 내부에는 `assets/`, `components/`, `composables/`, `config/`, `data/`, `layouts/`, `pages/`, `types/`, `utils/`를 사용한다.
- Pinia 상태가 필요한 경우에만 `app/stores/`를 추가하며, 페이지 지역 상태나 composable 공유 상태로 해결할 수 있는 경우에는 store를 만들지 않는다.
- `data/`는 앱에서 사용하는 정적 데이터와 데이터 정의를 둔다. 파일명은 반드시 `*.data.ts`로 통일한다.
- 명명된 TypeScript 타입과 인터페이스는 반드시 `app/types/`에 둔다. 구현 파일 내부에 선언하지 않는다.
- `config/`는 동작 설정과 환경별 구성값을 둔다. 비밀값은 클라이언트 코드나 `public` runtime config에 넣지 않는다.
- `app/types/common.types.ts`, `app/composables/useSetMeta.ts`, `app/config/site.config.ts`는 사이트 기본 설정 파일이다. 새 프로젝트 또는 사이트의 목적과 환경에 맞게 반드시 검토·수정하여 사용한다.
- 모든 페이지는 `useSetMeta`를 반드시 호출해 페이지별 제목과 URL을 설정한다. 홈 페이지는 `app/pages/index.vue`의 다음 형태를 기준으로 한다.

```ts
useSetMeta({
  title: '페이지 제목',
  url: '/',
});
```

- 디자인 작업 전에는 `app/assets/styles/`의 커스텀 Tailwind 토큰과 스타일 규칙을 먼저 확인하고, 임의 값보다 정의된 토큰을 우선 사용한다.
- 코드 들여쓰기는 스페이스 2칸을 사용한다. 빈 줄에는 공백 문자를 남기지 않으며, 연속된 빈 줄은 하나만 허용한다.
- 함수 선언과 호출에서 인자가 두 개 이상이면 인자마다 줄바꿈하고 스페이스 2칸으로 들여쓴다. 인자 자체가 함수인 콜백도 별도 줄의 블록으로 작성한다. 한 줄에 너무 많은 구문을 넣지 말고, 가독성을 위해 줄바꿈과 들여쓰기를 적극적으로 사용한다.

```ts
const func = (
  onAction: () => void,
  items: unknown[],
) => {};

runTest(
  'test',
  (test) => {
    console.log('this is test');
  },
);
```

## 컴포넌트와 UI

- `pages/` 컴포넌트는 라우팅, 데이터 조합, 메타 설정, 렌더링 컴포넌트 호출만 맡는다. 페이지 파일 안에서 직접 UI를 구성하지 않는다.
- 화면 UI는 목적에 맞는 렌더링 컴포넌트에서 구성한다. 예를 들어 홈 화면 UI는 `components/home/Home.vue`에 두고 `pages/index.vue`에서는 해당 컴포넌트만 사용한다.
- 모든 렌더링 컴포넌트와 UI 컴포넌트는 CVA 구조를 사용한다.
- CVA 클래스 배열에서는 함께 적용되는 Tailwind 클래스를 원소별로 분리하지 않고 하나의 문자열 원소로 결합한다. 예: `'text-md', 'bg-white'` 대신 `'text-md bg-white'`로 작성한다.

```ts
const cssVariants = cva(
  [
    '',
  ],
  {
    variants: {},
    compoundVariants: [

    ],
    defaultVariants: {},
  },
);
```

- 템플릿의 클래스 병합에는 `utils/cn`을 적극적으로 사용한다.

```vue
<header
  :class="cn([
    cssVariants({}),
    props.class,
  ])"
/>
```

- 컴포넌트가 외부 클래스를 받을 때에는 `class` prop을 정의하고 `cn()`으로 병합한다.
- 시간과 날짜 처리는 가능한 Luxon을 사용한다.
- 모든 상호작용 함수는 `on<액션><대상>` 형식으로 이름을 짓는다. `handle*` 명명은 사용하지 않는다.

## 상태, 데이터, 서버

- 상태는 페이지 지역 상태, composable 공유 상태, Pinia store 순서로 확장한다.
- Pinia store는 도메인별로 나누고, UI 컴포넌트가 store 구현 세부 사항에 직접 의존하지 않게 한다.
- Pinia store 파일명은 반드시 `*.store.ts`로 통일한다.
- Pinia store를 사용할 때에는 store 인스턴스를 먼저 선언한다. 반응형 상태·getter는 `storeToRefs()`로 구조 분해하고, action은 store 인스턴스에서 직접 구조 분해한다. 상태 변경은 특별한 사유가 없는 한 직접 대입하지 않고 action을 통해 수행한다.

```ts
const appStore = useAppStore();

const {
  isSidebarOpen,
} = storeToRefs(appStore);

const {
  onSetSidebarOpen,
} = appStore;
```

- 서버 API는 `server/api/`에 두며, 입력 검증과 오류 응답 형식을 일관되게 유지한다.
- 모든 API 성공·실패 응답은 HTTP 200으로 반환하고, 처리 결과는 공통 응답의 `error`, `code`, `message`, `details`로 구분한다.
- 클라이언트는 조회·변경·인증 API를 자동 재시도하지 않는다. API 실패나 전송 실패 뒤의 재요청은 사용자의 명시적인 행동으로만 시작한다.
- `any`를 사용하지 않는다. API, 폼, 데이터 경계에는 명시적 타입과 검증을 둔다.

## 검증과 Git

- 구현 완료라고 보고하기 전에 변경 범위에 맞는 검증 명령을 새로 실행한다.
- 기본 검증 후보는 `pnpm test`, `pnpm lint`, `pnpm exec vue-tsc --noEmit`, `pnpm build`다. 의존성이나 Nuxt 모듈 변경 뒤에는 `pnpm exec nuxi prepare`도 실행한다.
- 전체 검증이 기존의 무관한 문제로 실패하면, 변경 파일 대상 검증과 실패 원인을 분리하여 보고한다.
- 마스터의 요청 없이 커밋·푸시·PR 생성은 하지 않는다.
- 기본 브랜치는 `master`다.
- 모든 커밋 메시지는 `yyyy MMdd <type>: <한글 내용>` 형식의 날짜 접두와 커밋 타입을 사용한다.
- 변경사항이 많은 커밋은 본문에 불릿 목록으로 변경 내용을 상세히 기록한다.
